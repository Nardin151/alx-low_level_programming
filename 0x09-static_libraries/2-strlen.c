#include <stdio.h>
#include "main.h"
#include <stdlib.h>
/**
 * _strlen - returns the length of a string
 * @s: string of chars
 * Return : length
 **/
int _strlen(char *s)
{
int counter;
while (s[counter])
{
counter++
}
return (counter);
}
