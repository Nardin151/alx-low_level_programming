#include "main.h"
/**
 * _memset -  fill a string of text with a specific char
 * @s: string of char
 * @b: the character that we will fill with
 * @n: the length of characters that we will change to b
 * Return: new string s
 **/
char *_memset(char *s, char b, unsigned int n)
{
return (s);
}
