#include "main.h"
int Pali(char *s, int start, int end)
{

}
/**
 * is_palindrome - a function that returns 1 if a string is
 * a palindrome and 0 if not.
 * @s: String
 * Return: 1 - palindorm , 0 - not
 **/
int is_palindrome(char *s)
{
return (Pali(s, 0, int end));
}
