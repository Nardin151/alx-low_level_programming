#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 * Return: 0 = Success
 *_islower: checks for lowercase character.
 * c : character
 **/
int _islower(int c);
int _islower(int c)
{
char x = c ;
if (char.Islower(x))
return (1);
else
return (0);
}
