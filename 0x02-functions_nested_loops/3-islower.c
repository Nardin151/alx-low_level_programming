#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 * Return: 0 = Success
 *_islower: checks for lowercase character.
 * c : character
 **/
int _islower(int c);
int _islower(int c)
{
if (c == c.IsLower)
return (0);
else
return (1);
}
